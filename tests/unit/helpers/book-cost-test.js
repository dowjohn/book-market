import { bookCost } from 'bookmarket/helpers/book-cost';
import { module, test } from 'qunit';

module('Unit | Helper | book cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bookCost([42]);
  assert.ok(result);
});
